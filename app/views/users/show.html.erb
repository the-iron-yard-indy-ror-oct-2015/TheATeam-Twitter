<h1><%= @user.username %></h1>

<%= button_to "Follow", follows_path, :class => "btn btn-default btn-lg" %>

<div class="container">
  <% @user.posts.each do |x| %>
    <%= x.message %>
    <br>
  <% end %>
</div>

<div class="container">
  <%= link_to "Home", root_path %>
</div>
